services:
  postgres:
    image: postgres:17-alpine
    container_name: kivikova-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: kivikova
      POSTGRES_PASSWORD: kivikova_dev
      POSTGRES_DB: kivikova
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kivikova -d kivikova"]
      interval: 5s
      timeout: 5s
      retries: 5

  qdrant:
    image: qdrant/qdrant:latest
    container_name: kivikova-qdrant
    restart: unless-stopped
    ports:
      - "6380:6333"
      - "6381:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:6333/healthz || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: kivikova-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: kivikova
      MINIO_ROOT_PASSWORD: kivikova_dev
    command: server /data --console-address ":9001"
    ports:
      - "9002:9000"
      - "9003:9001"
    volumes:
      - minio_data:/data

volumes:
  pgdata:
    name: kivikova-pgdata
  qdrant_data:
    name: kivikova-qdrant-data
  minio_data:
    name: kivikova-minio-data
